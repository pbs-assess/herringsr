# Analysis and response

## Input data

Input data to the stock assessment are summarized in \autoref{tabDataInput}.
Relative to last year's assessment, the only change made to input data was updating the time series to include data from the \Sexpr{thisSeason} herring season (July 1 to June 30).

## Catch data

<!-- For the purposes of stock assessment, catch data are summarized by gear type and fishing category as described in \autoref{tabDataInput} and presented in \autoref{figCatchMajor}. -->
